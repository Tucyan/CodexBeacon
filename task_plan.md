# Desktop Dashboard 执行计划

## 当前开发进度（2026-09-05）

- [x] 新增版本化 JSON 备份：完整本地用户状态、自动周期、立即备份、导出/导入和旧设置兼容；排除 Provider/账户数据，导入前验证并事务保存。自动检查完成，文件对话框和实际恢复待用户确认。

- 已完成 Rust/MSVC 实际编译链接检查，rustc/cargo 1.98.0 可直接使用；rustfmt/Clippy 为可选项，未安装。
- 已初始化根目录正式 Tauri 2 + React/TypeScript 工程和 Git development 分支，复用本地依赖缓存，不覆盖原 PoC。
- 主 agent 实现窗口协调器、托盘、共享契约和集成；三个 subagent 分别实现前端、SQLite、Provider，并交叉审查。
- 首批正式功能已落盘：独立组件窗口、设置、业务组件、只读 Provider、保存与退出协议。Rust 22 项、前端11项测试通过，最终原生构建与双独立窗口/握手/SQLite/退出烟测通过；进程已关闭，可用正式启动脚本进入人工验收。
- 正式版本视觉/交互及015缩放验收仍由用户确认，019继续跳过独立实验。020/021原可行性结论保持，正式集成验收单独记录。
- 下方 2026-09-04 内容是历史交接点，不代表正式工程仍未初始化。

## 当前交接点（2026-09-04最终计划）

- 本轮已敲定[正式v0.1开发计划](最终开发计划.md)，覆盖原37节。本文下方为实验阶段任务和历史交接，不再以旧单窗口/019多窗口PoC要求约束后续开发。
- 用户明确确认020、021可行性通过；019跳过独立实验、不排期。正式架构固定为每组件实例一个独立Tauri窗口，无共同背景板。
- 012结合此前常规托盘通过和后续020恢复结果关闭；015缩放/坐标继续进行中，在正式Runtime阶段验收。
- 总表22项：20可行、015进行中、019跳过（按四状态约定显示未开始）。P0可行性准入已满足；正式应用尚未初始化，下一阶段为工程配置、共享契约及独立窗口壳。
- 继续由主agent统筹、subagent按模块并行、用户验证视觉交互；本轮未新增程序功能或重新运行实验。

**目标：** 在独立实验环境实现最小 PoC，自动验证非视觉行为；用户验证视觉与交互，主 agent 统一维护根目录验证表。

**架构：** `experiments/` 与未来正式工程隔离。Tauri 2 + React + TypeScript 为窗口实验，Node.js 原生模块为 Codex/Reset 协议实验，SQLite 数据实验单独隔离。原始凭据不读取、不记录。

## 阶段与分工

- [x] 0. 创建实验目录、记录环境，初始化依赖及启动命令（主 agent）：Rust/Cargo、MSVC/SDK 与原生 Debug 构建/隐藏启动已验证。
- [x] 1. Window PoC：001–008、012–015 的源码、原生构建、启动入口、实现报告与人工步骤已交付；人工结论待用户确认（窗口 subagent与主agent）。
- [x] 2. Codex PoC：009、010，真实只读数据和生命周期证据（Codex subagent），已复核。
- [x] 3. Reset PoC：011，真实公开 API、异常响应与缓存语义（Reset subagent），已复核。
- [x] 4. SQLite持久化、后台时间与资源采样：016/017/018均在记录范围内可行；Rust接入及Release资源属于正式开发验收。
- [x] 5. 检查所有证据，运行必要验证，交付人工测试入口并更新根表（主 agent）：50项本地测试与前端构建通过，独立代码复核问题关闭。
- [ ] 6. 用户提交视觉交互结果后，更新人工勾选与最终可行性结论。
- [x] 7. 需求变更决定：020/021通过，019按用户要求跳过；直接按独立Widget窗口架构制定正式计划。

## 边界

- 本轮维护最终计划，不编写正式应用。后续按正式计划进入工程初始化；不操作Explorer窗口树、不Hook Fences。
- 人工项未获用户明确确认时，保持“进行中”，不勾选、不判为可行。
- 长命令、下载、权限不足命令最多尝试一次；超时后交用户执行，不换方式重复。
- 非视觉实验遵循先写可证伪断言，再实现，再保存真实结果；模拟故障和真实账户/网络结果分开。
- 各 agent 只写自己的实验目录；根表与总体报告仅主 agent 修改。

## 已知环境问题

- 2026-09-03初检缺cargo/rustc；2026-09-04已安装并确认rustc/cargo 1.98.0，原生Debug构建成功。
- SQLite 初始夹具连接未关闭造成 Windows 文件锁，已明确 close 修正。
- Window dev server 127.0.0.1:1420 首次启动 EACCES；不重复同一命令，先定位端口保留状态。
- Reset 首次证据落盘因目录未创建失败；同时复核发现 fixture 错将 source 对象/active_watch可空对象写成字符串/boolean，已要求按真实schema纠正。
- 资源采样首次自检：PowerShell OrderedDictionary 用 Measure-Object -Property 聚合得到 null，改为逐项取数值聚合；首次自检文件不作为Dashboard资源证据。
- 独立review提出的Window缩放锚点、损坏持久化、状态恢复竞态和前端构建前置问题均已修复并复验。

## 实验阶段历史交接记录

- **2026-09-04 新增021可选渐显实验。** 用户不再追求全程不消失，要求设置复选框控制渐进出现。独立`experiments/show-desktop-fade/`保留E策略，subagent实现动画/设置，主agent负责构建与技术验证、报告及总表，视觉由用户验收。初始250毫秒缓出、默认开启；最终Tauri集成另行验证。
- **021技术检查已交付。** 一次离线构建成功（633A4BFF…35B781），8项决策+14项渐显/设置断言及E原生烟测通过；无自有实验进程，未启动长驻人工测试。入口`experiments/show-desktop-fade/Start-Fade.ps1`，待用户视觉反馈。旧A–E基线未变。

- **2026-09-04 用户已授权恢复分组实验。** 新建 `experiments/show-desktop-groups/`，用独立原生窗口对照 A跟随基线、B可最小化置顶、C不可最小化置顶、D非topmost相对定位、E按桌面状态临时topmost。主 agent 负责脚本/构建/日志/文档，subagent 实现窗口并独立复核，用户负责视觉交互。旧PoC保持退出，不恢复未审查的旧草稿。
- **A–E原生分组实验完成，程序均已正常退出。** D/E自动出现与Hide/Show通过；E首次进入短暂消失后出现、再次切换无闪，用户认为Show优于D。日志确认E退出桌面状态撤销置顶、Hide保持隐藏；首选E接入Tauri，D保留备选。输入和主动点击上移按用户接受范围记录，完整019/020集成仍待。
- 修订草稿保留在`experiments/window-poc/src-tauri/src/lib.rs`，配置`alwaysOnBottom=false`已改；**草稿未完成最终审查、未原生构建、未启动，不可直接称为修复版**。主agent最后一份大型补丁因上下文不匹配未应用。恢复时先重新完整读取源码，确认异步worker是否改为主线程执行、revision真正防抖、Hide/模式/退出失效、anchor实际使用与topmost防护、旧诊断菜单清理。独立审查结论见本任务subagent relative_desktop_review。
- 原生检查脚本`experiments/window-poc/scripts/Test-DesktopOrderRecovery.ps1`已创建且PowerShell语法检查通过，尚未实际执行。现有npm测试13/13通过，不代表新增Rust路径。完成源码审查后只尝试一次离线构建，再执行原生检查；长命令超时遵守用户规则交还执行，不自动重试。

- 本轮可自动执行的接口、SQLite、时钟逻辑及源码复核已交付。
- 以下为当时快照，最新统计见本文顶部及根总表：根表22项，17可行、5进行中；15项需人工确认，其中10项已由用户确认。
- 单窗口PoC继续作为基础能力证据；最终架构已改为每个Widget一个原生窗口，因此019/020需要新的实现和人工验收。
- 原生D/E两入口及Hide/Show已有成功证据；旧Tauri构建的失败保留为历史，不能覆盖新实验结果，也不能把原生成功写成Tauri已经修复。下一阶段用E原理接入独立组件，统一管理桌面检测与期望可见状态；验证自动出现/跟随系统切换、持久化、Hide失效门控、多组件和实际输入。旧D式Rust草稿须重读并按新选择改写，不直接执行旧相对层级自检作为E的验收。

## 验证命令与交付

- 各实验 README 提供精确执行命令，报告包含实现方式、环境、预期/实际、证据路径、限制。
- 根目录 `可行性验证.md` 是唯一进度总表。
- 人工结果格式：`VALIDATION-xxx：通过/不通过；现象；可选截图`。
