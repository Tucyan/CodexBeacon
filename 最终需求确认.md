# Codex Beacon v0.1 最终需求确认

更新日期：2026-09-14。状态：**需求口径已确认**。

完整范围、原计划对照、架构与阶段安排以[最终开发计划](最终开发计划.md)为准。用户已确认020/021通过、019跳过独立验证；本文件保留产品要求摘要。

## 1. 产品形态

- 应用只运行一个应用实例，由一个Rust主进程统一管理托盘、设置、组件窗口与服务。WebView2及自有Codex app-server可产生子进程，不承诺操作系统进程总数为1。
- 每个已启用组件都是一个独立的 Windows 原生窗口，例如 Memo 与 Todo 分别拥有自己的位置、尺寸、显示状态和缩放倍率。
- 不显示共同背景板，也不使用覆盖全部组件范围的大透明窗口。两个组件之间的区域完全属于桌面，桌面点击、右键、框选及 Fences 不应被应用拦截。
- 设置窗口是普通管理窗口；托盘负责显示、隐藏、锁定、设置和退出全部组件。

## 2. 组件窗口行为

- 组件窗口无边框、透明、无任务栏入口，默认定位在普通应用窗口下方。
- 组件不使用永久置顶模拟桌面常驻。用户主动点击组件后，允许其按普通窗口规则上移；暂不强制保持绝对置底。2026-09-04 用户明确认为该上移现象不重要，不作为当前验收阻碍；这不等同于已经实现点击后仍置底。
- 每个组件可独立移动、调整大小、显示或隐藏。操作一个组件不得改变另一个组件的位置和尺寸。
- Lock 同时锁定全部组件的拖动和原生 resize，但 Memo 文本、Todo 勾选及 Settings 仍可操作。
- 每个组件的有效缩放为 `全局倍率 × 组件倍率`，位置、逻辑尺寸、倍率、可见性及锁定状态在重启后恢复。
- 不同显示器拓扑分别保存组件位置和尺寸。拓扑至少区分分辨率、DPI缩放、显示器排列和主屏；首次切换到新拓扑时从离开前的布局映射继承并保持组件完整可见，之后切回时恢复该拓扑自己的布局。

## 3. 透明区域与鼠标输入

- 组件窗口边界应尽量贴合组件内容，避免形成较大的透明边距。
- 组件窗口外的区域必须正常操作桌面与 Fences。
- 组件自身矩形窗口内的透明像素可以不穿透鼠标；这是已接受的产品限制，不要求逐像素命中测试、窗口区域裁剪或局部鼠标穿透。

## 4. “显示桌面”设置

设置中提供一个全局选项“显示桌面时”，至少包含以下两种模式：

| 模式 | 行为 |
| --- | --- |
| 显示桌面时自动出现 | 点击任务栏最右侧“显示桌面”或按 `Win+D` 后，原本应显示的组件允许短暂消失，随后不抢焦点地自动出现；离开显示桌面状态后恢复默认层级，用户随后主动点击组件允许其上移 |
| 跟随系统 | 点击“显示桌面”或按 `Win+D` 后，组件随普通窗口隐藏；再次切换显示桌面时恢复原可见状态和布局 |

建议默认使用“显示桌面时自动出现”，该设置需持久化，并同时作用于所有组件窗口。

托盘 Hide 和组件自身的关闭/禁用状态始终优先；“显示桌面时自动出现”只恢复应用状态中本来应该可见的组件，不能把用户主动隐藏的组件重新弹出。v0.1 的这一设置只承诺覆盖任务栏最右“显示桌面”和 `Win+D`，不扩展为虚拟桌面、锁屏或全屏应用的常驻承诺。

公开 Windows API 没有为普通置底窗口提供明确的“排除 Show Desktop”开关。首轮仅监听最小化且维持绝对置底已在两入口失败。2026-09-04 独立原生分组实测确认D（非topmost相对定位）和E（按桌面状态临时置顶）均能自动出现；用户认为E的Show表现优于D，因此后续接入首选E、保留D备选。

首选实现：用自有隐藏参照窗口与只读桌面宿主层级检测显示桌面状态；期望可见的组件在进入该状态时临时置顶，退出时撤销置顶并恢复默认层级，定位使用不激活标志。Hide/关闭/模式切换优先于自动恢复。仅操作本应用窗口，不修改Explorer窗口、不使用私有消息、注入或WorkerW/Progman嵌入。检测器、临时置顶及恢复任务须统一管理并在退出清理。此原理已在独立原生PoC验证，Tauri多组件集成尚待；实现不限于原草稿的前台hook方案。

### 4.1 可选渐显动画

2026-09-04 用户确认不再继续追求显示桌面过程中全程不消失，要求将恢复时的突然出现改为可选渐显。

- 设置增加“启用渐显动画”勾选项，全局生效并持久化。采用已获用户021通过确认的默认勾选、约250毫秒缓出，首版无需额外时长滑块。
- 在“显示桌面时自动出现”模式恢复组件，以及托盘将主动隐藏的组件重新 Show 时渐显；已显示组件的重复 Show、周期检测及退出显示桌面状态不重播动画。
- 取消勾选时直接出现；动画过程中关闭该选项，立即结束动画并恢复目标透明度。
- 动画只改变临时的整体可见度乘子，终点为组件原有外观，不修改用户保存的背景或文字图标透明度。
- Hide、关闭、禁用和切换为“跟随系统”优先，取消尚未完成的恢复/动画；不能由过期动画重新显示组件。隐藏立即生效，不增加淡出等待。
- 渐显用于改善恢复观感，不承诺消除Windows先隐藏窗口产生的短暂空档。最终Tauri多窗口需独立验证合成、焦点与生命周期。

## 5. 人工验收口径

2026-09-04 分组实验已完成：B/C的常驻置顶仅为对照，E仅在显示桌面期间临时置顶。根据用户对自动出现与Hide/Show的反馈，E列为后续集成首选，不将常驻置顶改为产品要求；原非topmost限制保留于D备选路线，不能继续作为禁止E接入的旧约束。

后续D组验收更新：两入口非topmost自动出现、Hide优先和Show恢复已获用户确认。用户接受主动点击D导致Z序上移覆盖普通应用，不将此项作为阻碍；强制置顶应用遮住D时可正常输入，仅记录该实际测试范围。最终Tauri多组件仍需验证输入及模式切换，不能把原生测试扩大为所有焦点情形通过。

- 启动后只看到各独立组件，不出现共同背景板。
- 在组件之间的区域点击、右键和框选时，桌面/Fences 正常响应。
- Memo 与 Todo 分别移动和 resize，互不影响；在当前显示缩放下无明显坐标漂移。
- 托盘的 Show、Hide、Settings、Lock、Unlock、Quit 能统一管理全部组件，Quit 后应用进程退出。
- 两种“显示桌面”模式分别测试任务栏最右按钮与 `Win+D`；“显示桌面时自动出现”模式允许组件短暂消失，自动恢复不得抢焦点；主动点击组件后的层级上移按上述接受口径处理。
- “显示桌面时自动出现”模式下，托盘 Hide 后组件必须保持隐藏，不得被最小化监听逻辑重新显示。
- 退出并重启后，各组件布局及“显示桌面”设置正确恢复。
- 勾选渐显时，两种入口的自动恢复和 Hide 后 Show 呈现平滑渐显；取消勾选后直接出现，重启保留选择；动画期间 Hide 不得反弹显示，原有背景/内容透明度不变。

## 6. 对原计划的修订

以下原设计约束由本需求替代：

- “所有组件只能存在于一个 Dashboard Window”改为“每个组件一个独立原生窗口”。
- “不处理 Win+D/显示桌面行为”改为“由设置在显示桌面时自动出现和跟随系统之间切换”。

当前单窗口PoC保留为基础能力证据。019已由用户豁免，不再要求新增多窗口PoC；正式工程直接采用独立组件窗口，最终产品验收检查其集成行为。

## 7. 已确认口径

“显示桌面时自动出现”允许组件在 Show Desktop 发生后短暂消失，随后无焦点恢复；不要求整个系统动画期间持续可见。首轮继续采用公开窗口恢复路线，无需为“全程不消失”引入更深的 Windows 桌面层方案。

## 8. 本地备份与迁移

- 自动备份默认每7天执行一次，设置中允许1–365天的整数间隔，也提供立即备份。
- 备份覆盖主题、行为、组件启用/可见状态、各显示器拓扑的窗口布局及Memo/Todo/Countdown内容。
- 支持通过Windows文件对话框导出和导入版本化JSON。导入会替换上述本地状态，执行前必须明确提示。
- 备份禁止包含Codex/Provider状态、账户响应、访问凭据和原始网络响应。
- 标准备份保存在应用数据目录并保留最近10份；用户导出的JSON由用户自行管理。
