# 开发与实验进度

## 2026-09-05 01:39 界面精简与Reset语义

- 去除卡片外侧阴影，Usage仅保留账户/credits/两额度窗口，Memo改为单滚动容器，全局滚动条收细并去箭头。
- 实查Reset主页/当前API/OpenAPI：公告追踪另带AI预测，无投票数据依据。卡片区分赠送次数与额度重置公告，未过期预测另列且标注非官方；当前API预测为空。
- 前端15测试、Rust23测试、Vite及原生构建、多窗口回归烟测通过；重启正式版供用户验证视觉。实现与来源见`docs/development/UI-refinement-2026-09-05.md`。

## 2026-09-05 01:09 组件串窗修复

- 用户反馈Widget unavailable与正常页面高频交替。定位为Tauri `window.emit`全局发送 + 前端Any监听，将其他窗口的scoped快照送入当前组件。
- state/appearance/flush改为定向发送和监听；新增回归由失败转通过。12项前端测试、正式构建及连续12次原生move/resize的双renderer身份检查通过。
- 已重新启动修复版PID119832，等待用户视觉复测。见`docs/development/FIX-window-events-2026-09-05.md`；现行源码与二进制已更新，旧00:49烟测仅作历史。

## 2026-09-05 正式开发启动

- 环境复核：Rust/MSVC真实编译链接运行通过；无需重装或补充阻断配置。仅rustfmt/Clippy可选未安装。
- 正式根工程、共享TS/Rust模型、权限、独立窗口/E策略/托盘、SQLite及Provider已实现；三个subagent负责前端、存储、Provider并交叉审查。
- 本地缓存完成npm安装；正式Rust22测试、前端11测试及TypeScript/Vite通过；Debug原生构建成功。报告见`docs/development/REPORT-2026-09-05.md`。
- 00:49正式集成烟测通过，独立临时数据目录中两个HWND/两个renderer就绪，SQLite及flush退出正常；SHA256 `24B7B5D5027E41E4507D2D7A1E9DFC7FF8348A79A8BA5F6304B83B8B9BFDF1A5`。程序已关闭，正式启动入口`scripts/Start-DesktopDashboard.ps1`。
- 本轮未将自动测试冒充视觉验收，015继续进行中，019继续跳过；接下来由用户验证正式版本组件布局/透明度/缩放/显示桌面，随后进行完整在线Provider及Release集成。

## 2026-09-04 最终计划敲定

- 用户明确确认020与021可行性通过，019无需独立验证并跳过；重申各组件独立存在/调整位置，无共同背景板。
- 根总表同步020/021可行[x]、019跳过未开始/否/—；012此前的Show异常阻塞由后续恢复反馈解除，合并原常规托盘确认关闭。015保留进行中。
- 主agent对照原计划37节，发布根`最终开发计划.md`入口与`docs/superpowers/plans/2026-09-04-desktop-dashboard-v0.1.md`完整计划；同步最终需求、实验报告和交接。
- subagent独立审计Codex/Reset/SQLite证据，澄清通知/请求、派生remaining、banked语义、usage字段与Rust尚未接入的边界。正式应用尚未初始化；下一阶段按最终计划执行，不再要求019前置PoC。

## 2026-09-04 可选渐显实验

- 21:15:53 用户要求启动，已运行E+渐显版PID114224，日志`group-E-20260904-211553-656.jsonl`持续记录。原生初始状态正常、渐显开启；等待用户两入口及开关观感反馈，021未勾选。

- 用户不再追求全程不消失，新增Settings“启用渐显动画”复选框需求；根目录最终需求与VALIDATION-021已同步。
- subagent实现独立`experiments/show-desktop-fade/Probe.cs`，主agent完善取消顺序、退出清理、真实测试和隔离启动脚本。E策略保留，默认250毫秒缓出，JSON保存开关；不改旧Tauri数据。
- 一次离线构建成功，SHA256 `633A4BFFE535448BE6753DFE32E53FA876D15376207941DA9477FBB96035B781`。8项决策断言、14项渐显/设置断言及E原生烟测全部通过，原生SetWindowPos失败0次；技术测试退出后无实验进程。
- 真实两入口渐显观感、Settings交互和重启保持需用户确认，021保留进行中。长驻人工实验尚未启动；入口`experiments/show-desktop-fade/Start-Fade.ps1`，实现方式与证据见其REPORT.md。最终Tauri集成仍待。

## 2026-09-04 本轮诊断反馈

- **暂停交接：用户先休息，明早再继续。** 不再构建或启动程序。旧PoC退出0、采样器到时正常结束，实施subagent已中断。相对桌面层级修订源码为未验证草稿，root最后整块补丁因上下文不匹配没有应用；明早先完整审查当前文件，再处理主线程/防抖/门控/anchor边界。新增原生检查脚本尚未执行；今晚现有npm13/13通过，不能替代新Rust验证。

- 用户最终纠正：Win+D也失败，此前正常为观察误差。Win+D区间176条状态记录均非最小化/cloak0，58条桌面在main之前；与任务栏路径一致。已撤回当前文档的Win+D正常结论，保留历史调查。采样器03:14:38按600秒上限正常结束；用户托盘退出PID114252，退出码0。开始并行实施/审查公开GetShellWindow相对层级恢复，不再只依赖最小化。

- 用户确认PID114252任务栏Show后仍不可见，视觉失败与已捕获桌面移到组件前方的时段一致。当前自动逻辑仅接收main最小化开始且恢复前要求iconic，未覆盖本次非最小化桌面层级变化；Show又维持绝对置底。Win+D成功原因仍缺同轮日志，已请求在当前采样器运行期间做一次对照，未断言两入口的Windows内部机制固定不同。

- 连续采样捕获03:05:30 Progman从组件后方移到前方（桌面rank24、main375）；03:05:34 Show后main获得焦点但仍在桌面之后（376/25）。03:05:36切回Chrome后桌面返回组件后方。同期main/子窗口矩形正常、visible1/cloak0，main非最小化。原始对照存validation020-desktop-order-comparison.json；这是层级遮挡假设的同期证据，不等于已证明恢复方案。当前轮视觉反馈待用户同步。

- 用户补充确认：启动时可见，任务栏按钮后消失，Show/取消置底/恢复置底均完全不可见。对照失败已同步总表，020未通过。新增外部只读连续采样器，2秒8次真实采样自检通过；沿用同一exe启动PID114252，03:04:38开始250ms采样（上限600秒），采集位置、DWM cloak、子窗口、前台和Z序以覆盖故障现场，未改恢复策略。

- 用户确认任务栏显示桌面未自动出现，并依次操作Show、取消置底、恢复置底和Quit。日志收到全部托盘动作，各窗口调用返回Ok，状态采样为visible=1/iconic=0；取消置底后实际是否出现仍待用户补充，不据API状态判定视觉通过。
- 普通最小化自检之后没有新的过滤后主窗口minimize日志，尚不能据此确定Shell根因。Quit日志及hook卸载成功，退出码0；退出后根进程及应用标识/直接父PID匹配WebView均0，证据存入validation020-quit-check.json。018标为有限范围可行，012/020因异常恢复路径继续进行中。

## 2026-09-03

- 用户授权主 agent、subagent、用户并行实验；视觉交互由用户验收。
- 已读取原开发计划和执行约束；工作区初始为空。
- 开始初始化独立实验环境；记录基础运行时，调查 Rust/构建工具可用性。
- 已创建 Python venv；三个 subagent 分别负责 Window、Codex、Reset，主 agent 负责环境、SQLite/后台逻辑和总表。
- 系统工具链检查确认 Rust 缺失、vswhere 返回空实例列表、本会话非管理员。已准备一次性用户安装脚本；原生 Windows PoC 编译需此条件满足。
- SQLite/后台计时先写 12 个实验断言，首次运行确认实现缺失。发现测试夹具的 sqlite3 连接上下文不自动 close，导致 Windows 临时文件锁；已定位并改用 contextlib.closing（文件占用，不是系统权限不足）。
- SQLite/后台逻辑 12/12 通过，报告与证据已保存。016/017 按有限实验范围标为可行；018 已有采样脚本，原生进程待构建。
- 窗口端口1420的EACCES定位为既有PID7256占用；静态配置改5173，启动交用户，不终止占用进程。统一入口 Run-WindowPoC.ps1 与人工验收说明已写。
- 主agent复核发现并要求修正Window原生resize/lock/缩放与Reset真实schema差异；Codex修正UTF8、错误脱敏、只读白名单与直接exe生命周期。
- Reset修正后单次短请求HTTP200，证据成功保存且同body经Provider校验。旧请求没有超时，初次失败是写盘ENOENT；不把该本地问题推给用户。
- 根表19项格式校验通过，所有人工项保持进行中且未勾选。尚未收到任何用户视觉结果。
- Codex证据审查移除原账户探针未实测的timestamp/cleanup/limitId补录值（保留null与来源说明）。另行完成直接vendor exe真实生命周期；signalCode漏判已用失败fixture修正，旧真实证据不改写。
- Codex纯本地15项通过；Reset纯本地12项通过且真实HTTP200证据复核通过。009/010/011已按报告限定范围标为可行。
- 独立review agent进行规格与代码质量检查；Window仍在补齐独立倍率、带倍率的西/北边resize，以及修正手动编号与总表的一致性。
- 统一验证调用的首次日志路径因被子脚本Push-Location影响而解析错误；日志目标改为调用前确定的绝对路径后，整套命令正常执行。
- 2026-09-03 22:10（Asia/Singapore）：统一本地49项通过，tsc/Vite build通过；日志 experiments/reports/local-verification.log。不包含任何原生构建或用户视觉通过结论。
- 修复独立复核的两项Window问题后，主agent只复验变更的Window：11/11与tsc/Vite build通过；最终套件合计50项。新增Reset layout入口、修正Fences测试流程并明确倍率恢复方法。
- 最终独立复核确认两项必修关闭。末次环境检查仍无cargo，Tauri CLI局部依赖已存在；用户需完成系统工具链安装，随后继续原生构建/人工验收及资源测量。
- 本轮交付状态：19项中5可行、14进行中；所有人工勾选为空。根表、实验实现、报告与执行脚本均已保存。
- 用户提供系统安装结果后，只读确认 Build Tools 17.14.39 / MSVC 14.44.35207 / SDK 10.0.26100.0 已安装；Rustup 1.29.1 能运行，但无已安装/默认工具链。cargo/rustc 长度 0 实为符号链接，并非损坏证据。
- winget 日志确认下载器曾超时，安装器随后下载成功；rustup-init 运行约 81 分钟后退出 1，未记录 stderr，工具链缓存留有 .partial。根因尚不能最终断定。日志副本、诊断和只补装工具链的日志脚本已准备，下载交用户执行，未重复安装。000 保持进行中，人工状态不变。
- 新补装脚本与 PoC 启动前检查通过 PowerShell 语法校验；原日志/证据副本 SHA256 一致；19 行与 12 个人工未勾选项校验通过。独立 review subagent 复核补装脚本和诊断报告无阻碍；未执行安装。PoC 启动器现在会禁用隐式安装并检查 cargo/rustc 版本，避免误将 Rustup 代理文件存在判为环境就绪。

## 2026-09-04

- E用户确认两入口首次短暂消失后出现、再次切换不闪烁；Hide/Show通过且Show比D更正常。487条sample中14条期望隐藏（8条Active）均不可见，SetWindowPos失败/限次记录0；可见状态退出Active后撤销topmost并置底。E列为Tauri接入首选，D备选；最终需求/根表/报告已同步。E正常WM_CLOSE退出0，本实验和旧PoC进程0。A–E核心分组完成，019/020完整Tauri集成未完成。

- 用户确认D的Hide优先及Show恢复，并接受主动点击D导致其Z序上移覆盖普通应用；仅确认强制置顶应用遮住D时输入正常，不扩展为全部焦点场景。848条sample中31条期望隐藏（17条Active）均native不可见、全体非topmost。根需求同步点击上移接受口径；D正常退出0，E组PID116228启动，待临时置顶方案对照。020整体因E/最终Tauri集成未完成仍进行中。

- 用户明确确认D两入口均在第二次按键/点击前自行重新出现。D非topmost自动出现核心能力标可行，时序确认写入原始分析JSON与报告；020整体不勾选。D PID112556保持运行，下一步人工验证普通应用覆盖、焦点、Hide后两入口保持隐藏及Show恢复；E/Tauri集成尚待。

- D用户连续操作两入口均观察短暂消失后出现；保存208条sample中8条Active、0条topmost；每次相对定位后下一次约250ms采样组件已在桌面之前、检测器仍Active，后续退出Active恢复置底。需明确视觉出现是否在第二次切换之前，覆盖/焦点/Hide亦待验收。D PID112556继续运行，未切换E，020保持进行中。

- 用户先说明C组连续操作2次Win+D、2次任务栏按钮，随后明确四次均始终可见。记录为同一运行两入口测试而非独立重启；保存180条sample中7条Active，均置顶、非最小化/cloaked且组件在桌面之前，无自动恢复。C基础现象勾选，C正常关闭0后D组PID112556启动，等待D/Win+D非置顶相对恢复观察；020整体未通过。

- 用户确认B/任务栏按钮持续可见；219条sample内2条Active均保持置顶且未最小化、组件在桌面前方，B两入口基础现象勾选。B正常关闭0，C组PID119648已启动待Win+D；原生样式核对B/C仅WS_MINIMIZEBOX不同、扩展样式一致。020整体继续进行中，常驻置顶不是最终策略结论。

- 用户确认B/Win+D持续可见。345条sample中3条Active均保持置顶、允许最小化样式且未最小化/未cloaked、组件位于桌面前方；仅两次启动原生调用，无周期性恢复。记录该样式组合不依赖禁止最小化即可在本入口保持可见，不泛化浏览器。旧B正常WM_CLOSE退出0，新B PID120332待任务栏最右按钮；分次人工勾选已维护，020整体仍进行中。

- 用户确认A/任务栏最右按钮与Win+D相同：消失不自动出现，切回应用后出现；178条sample内3条Active均非最小化、非cloaked且桌面在组件前方。A基线两入口勾选，附加交互未认定通过，020整体仍进行中。A正常WM_CLOSE并实际取得退出码0；新增限当前记录归属的正常关闭脚本。B组PID114516已启动，原生检查置顶且允许最小化，等用户B/Win+D反馈。

- 用户确认A/Win+D：按后消失且不自动出现，点击任务栏任意应用后出现。保存596条sample分析，其中Active的15条均非最小化、cloaked=false且桌面在组件前方；程序无自动恢复动作。本入口基线确认，020整体不勾选。旧A通过验证HWND的WM_CLOSE正常结束（helper退出码null保留），A独立重启PID115652，待任务栏显示桌面入口；后续helper增加进程句柄保留以改善退出码采集。

- 用户授权恢复020分组实验；建立独立 experiments/show-desktop-groups，主 agent编写脚本/文档、subagent编写Probe.cs，另两轮只读审查。修正观察标记副作用、B/C样式混杂、managed Show定位顺序与D退出恢复。新增原生位置断言先在A失败，再修正共享启动路径后通过。
- 最终离线C#构建SHA256 627E0F6AD38C88ED9DAF2880F7B1CB748CD8E6FEABFF6FEFAC8D27F63AD2A7CD；8项决策断言、A–E五组有限原生检查通过，日志/限制见新REPORT。一次启动helper因Windows PowerShell缺Get-FileHash短时失败，改用.NET SHA256后成功；19:39:44启动A组PID110632，持续JSONL和退出记录就绪。等用户A/Win+D视觉结果；不把技术检查当020通过。

- 后续暂停实验期间，按用户新请求并行审查 OpenPets、DSH、VPet、Shimeji 与 Rainmeter。主 agent 复核 Rainmeter 固定提交，纠正 subagent 摘要遗漏的“显示桌面期间临时topmost”细节；保存《VALIDATION-020-开源方案调研》，同步根表。未构建、启动或运行第三方程序；020仍进行中、人工未确认，运行实验仍暂停。

- 用户报告 Rust 编译器下载约 5.01 KiB/s、68.01 MiB、ETA 3 小时。核对 TUNA/Rustup 官方文档后，提供用户停止原下载并通过当前终端 RUSTUP_DIST_SERVER 临时切换镜像的方法。未中断用户进程、未自行重试下载或测速；环境仍进行中。
- 用户 00:37 的新日志表明半安装目录被 --no-update 跳过；本机确认缺 rustc.exe、manifest、config，仅 cargo 已落盘。修订脚本将缺失目录保留到同一 Rustup 根目录备份区后再安装，内置 -Mirror Tuna 并去掉 --no-update。
- 离线检查首次误调用旧脚本，因未声明 CheckOnly 参数导致一次隔离 RUSTUP_HOME 的元数据请求，已终止确认为本次测试所有的子进程并保留日志。测试加入 AST 参数守卫后先验证失败，再修订脚本；四项恢复计划检查通过，本机 CheckOnly 正确识别三个缺失文件。未恢复或删除用户真实工具链，实际安装待用户执行。
- 独立 subagent 确认修复方案，并提出提前检查重解析点、更新旧说明两项反馈，已落实；Windows PowerShell 5.1 离线检查通过。修订脚本和用户入口已统一为 -Mirror Tuna。
- 用户通过修订脚本成功安装 stable MSVC 工具链：rustc/cargo 1.98.0；关键文件、默认工具链和旧目录备份均确认正常。
- 首次原生构建在项目build script因缺icon.ico失败。新增确定性ICO生成器与格式检查；随后离线增量原生构建成功。独立复核发现窗口状态恢复/立即保存竞态和缺beforeBuildCommand，已修复；窗口11/11、前端构建、最终离线原生构建通过。
- 最终window-poc.exe为13,194,240字节。隐藏启动并采样8秒：8进程，Private 219.61–231.17MiB，Working Set 461.10–466.40MiB；强制终止本次根PID后已采样PID残留0。018仍待用户托盘Quit，不把短时Debug样本称为轻量或长期稳定。000更新为可行；当前6可行、13进行中。
- 用户在根表人工勾选001–008和014；已规范为[x]并按规则标为可行。用户说明008的Reset layout存在功能问题但不影响整体验收，记录为非阻断已知问题，具体现象待补充。012/013测试含义已展开为可操作步骤；015仍待确认。当前15项可行、4项进行中。
- 用户复测Reset layout已恢复正常，008不再保留当前缺陷。013确认透明空白区鼠标不会穿透，底层桌面/Fences输入被拦截，接受性仍待决定。用户称托盘正常，但启动PID 117376仍在运行，因此012先记录功能正常、Quit及退出码待最后确认；018保持进行中。
- 用户澄清最终产品不应有共同背景板，每个Widget应独立存在；在此前提下接受Widget自身透明像素不穿透。用户新增可配置Show Desktop行为：当前PoC会被任务栏最右按钮隐藏/最小化，需增加“显示桌面时自动出现”模式。开始并行研究Tauri多窗口结构与公开Win32恢复机制，暂不修改PoC实现。
- 已形成根目录《最终需求确认》：一个进程统一管理、每个Widget一个独立原生窗口、组件外区域完全属于桌面、组件矩形内透明像素可不穿透；Show Desktop提供“显示桌面时自动出现”与“跟随系统”两种全局模式。013按用户接受结论标为可行，新增019多窗口与020显示桌面验证。
- 两个只读subagent复核完成：多窗口应由Rust/AppHandle统一协调并按Widget label管理；Show Desktop首选公开WinEvent检测与无激活恢复，托盘Hide优先，WorkerW/Progman及Explorer私有消息不进入首版。工作区不是Git仓库，因此本轮无法提供git diff，已直接校验21行验证表状态统计。
- 用户确认“显示桌面时自动出现”允许组件短暂消失后无焦点恢复，不要求在系统Show Desktop动画期间全程可见；相关需求、验收描述和020备注已同步更新。
- 开始020单窗口机制实验：按TDD先加入模式解析/恢复门控断言，首次因实现模块缺失得到预期RED，最小实现后13/13 GREEN；TypeScript/Vite生产构建通过。Rust加入SetWinEventHook、140ms异步SW_SHOWNOACTIVATE恢复、HWND_BOTTOM重设、重入保护、用户Hide门控和退出卸载；规格审查通过。
- 原生cargo quick-check在一次允许的30秒窗口内未完成，已停止且不由agent重复。cargo fmt检查也因当前toolchain未安装rustfmt而不可用，不为格式检查触发额外下载。已新增离线、单次、带日志的`experiments/Build-And-Run-Validation020.ps1`交用户完成原生编译和可见验收；020保持进行中。
- 用户随后明确要求主agent启动020。首次完整构建以Tauri Builder不存在`on_event`失败；按本机Tauri 2.11.5源码改用`Builder::build`后，第二次因遗漏`let app`绑定失败。补齐绑定后第三次离线构建成功（总耗时28.00秒，Cargo 22.69秒），产出13,215,744字节Debug exe并启动PID 109424。失败均为约8秒内明确编译诊断，并非超时或下载重试；最终日志与JSON证据已落盘。当前等待用户完成020视觉/交互及托盘Quit验收。
- 用户实际测试任务栏“显示桌面”和Win+D均未达到自动出现，Show Desktop后托盘Show也无效。现场原生探针确认目标窗口Visible=true、Iconic=false、DWM Cloaked=0、矩形位置正常；首次探针的无符号style转换及首轮Z序脚本分别出现PowerShell诊断脚本类型错误，纠正后取得有效状态与Z序数据。当前假设转向有效Z序/桌面合成问题，已执行一次无激活HWND_TOP诊断，等待用户确认是否重新可见；020继续进行中。
- 后续诊断版移除自动恢复后的绝对HWND_BOTTOM重排，并让托盘Show先unminimize；已构建启动PID 113572。用户最新纠正：Win+D正常，之前由热键冲突造成异常。已更新总表与报告，撤回“无事件日志即可证明Shell阻止恢复”的推断。外部前台监视器日志仅有02:38:20–02:40:20起止记录，不能作为无系统事件证据。等待任务栏最右“显示桌面”和随后托盘Show的最新结果，未将020整体标为通过。
- 用户后续确认任务栏按钮仍失败且Show不能恢复；旧PID 113572因误关终端以-1073741510退出，不计作Quit通过。并行审查确认Tao always-bottom会覆盖外部HWND_TOP请求，因此此前置顶探针不足以排除置底因素。新增时间戳JSONL文件日志、托盘操作结果与延迟状态采样、取消/恢复置底对照菜单。一次离线构建成功（Cargo 11.20秒），独立隐藏helper启动可见PoC PID 113548。普通最小化正向对照捕获event22并恢复iconic=0，采样前台未变化；任务栏真实操作仍待用户观察。报告已记录实现和证据。
## 2026-09-05 配置备份

- 新增备份设置页：自动间隔1–365天（默认7）、立即备份、导出JSON、导入JSON、最近备份状态。
- 版本化JSON只保存settings/widgets/content；Provider、账户响应、凭据与运行revision排除。
- 后端使用Windows原生JSON文件对话框；导入先flush、限制32MiB、严格反序列化、正式模型校验及SQLite保存，再替换内存状态。
- 自动备份启动/每小时检查，dirty清空后按间隔执行，只保留最近10份自身标准文件。
- Rust 34项、Node 18项、TypeScript/Vite、离线原生构建及多窗口smoke通过；正式启动真实生成默认7天的首份备份。人工导入/导出对话框和恢复结果列入VALIDATION-023。

## 2026-09-05 便携 ZIP

- 新增可重复执行的 `scripts/New-PortableRelease.ps1`，核对三处版本号后生成 Windows x64 Release EXE、便携说明、版本清单、ZIP 和 SHA-256。
- 首次调用因脚本开关传递错误误触发Debug构建，被正在运行的正式Debug程序文件锁拒绝；未覆盖程序。修正为显式 `-Release` 后完成唯一一次Release全量优化编译。归档首次因 `Compress-Archive -LiteralPath` 不展开通配符失败，改为 `-Path` 后只重新归档，没有重复编译。
- Node 18项、Rust 34项、TypeScript/Vite和Release构建通过；Release原生smoke通过。ZIP三项内容检查及哈希一致，禁止内容0项。
- 交付 `release/Desktop-Dashboard-0.1.0-windows-x64-portable.zip`，SHA-256 `5E5272DE737A39C9013B840DE570789990F91B6D90FD3C3B88B44F06B0E36DF7`。当前未签名，用户数据仍位于Roaming应用数据目录。
