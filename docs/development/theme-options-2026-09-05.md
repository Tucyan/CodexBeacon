# 配色方案草稿与额度进度条

日期：2026-09-05。状态：用户选择方案 A；正式额度组件使用剩余额度进度条。

## 配色草稿

文件：[`../design/theme-options.html`](../design/theme-options.html)。独立 HTML，内联 CSS/JavaScript，无外部依赖，可直接用本机浏览器打开。

| 方案 | 实现方式 | 取舍 |
| --- | --- | --- |
| A：5 个基础色 | 卡片、输入区、正文、次要文字、强调色；边框混色生成，按钮继承强调色，按钮文字按亮度生成 | 设置较少，自动生成部分不能单独调整 |
| B：8 个界面用途色（建议） | 在 A 的基础角色上分别设置边框、按钮背景、按钮文字 | 解决输入区、提示文字、按钮混用颜色的问题，仍统一管理 |
| C：全局主题＋组件覆盖 | 8 个全局用途色，允许 Todo/Memo/Usage 分别覆盖背景与强调色 | 适合壁纸不同区域，设置更多 |

每个方案保存独立的页面会话状态；支持颜色选择器、六位 HEX（可带 #）、三组预设、背景透明度、四种预览背景与 JSON 下载。示例组件互不依附统一背景板，Todo/Memo 可编辑；Usage 数据为标明的演示数据。刷新恢复默认，不写正式软件设置。

## 正式进度条

`src/widgets/CodexUsageWidget.tsx`：Primary/Secondary 使用原生 `<progress max={100}>`；值取 `100 - usedPercent` 并限制在 0–100，缺失数据不伪造进度。标签说明为 remaining，保留紧凑的已用/剩余文字与本地重置时间。

`src/app/styles.css`：4px 圆角轨道，强调色填充，淡色轨道。没有恢复 Ready、token 汇总等冗余信息，也没有修改正式主题数据模型或已有用户配置。

## 验证与交付

- 前端代理执行 `npm run build`（含 TypeScript 检查）：通过。
- HTML 内联 JavaScript 语法、HEX 接受 `#FFFFFF` / `123456` / `#12AF90` 并拒绝非法值：通过；检查中修正了 HEX 正则转义。
- 内置浏览器的本地文件 URL 被浏览器安全策略禁止；未绕过限制。HTML 尚未完成浏览器视觉及交互验收，由用户本机浏览器查看选择。
- 用户确认退出后，离线原生构建一次通过（8.85 秒）；保留现存 unused/dead-code 编译警告。证据：`evidence/usage-bars-build-20260905-015210-877.json`。
- 已重新启动正式程序，PID 96652。二进制 SHA256：`2AD16598059B03E125F7CDF72500ED02B0F15CE20CC66AF60B1CF3A49B3D3640`。
- 本次不将任何视觉可行性项目标记为人工通过；等待用户实际观感反馈。
